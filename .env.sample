# UniFi controller URL (e.g. https://192.168.1.1 or https://unifi.yourdomain.com)
UNIFI_HOST=

# API key generated from UniFi Network Settings > API
UNIFI_API_KEY=

# Default site ID (found in UniFi URL or via the list_sites tool). Optional if passed per-call.
UNIFI_SITE_ID=
